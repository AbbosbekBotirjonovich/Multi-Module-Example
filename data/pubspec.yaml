name: data
description: "A new Flutter package project."
version: 0.0.1
homepage: none

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  injectable: ^2.5.0
  get_it: ^8.0.3
  remote:
    path: source/remote

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  injectable_generator: ^2.7.0
  build_runner: ^2.4.14

flutter:
  uses-material-design: true